# Cortex — dependencies (pinned for reproducible installs and CVE tracking)
#
# Refresh policy: run `pip-audit -r requirements.txt` and bump pins quarterly,
# or sooner when a CVE is disclosed. Keep minor/patch upgrades within a
# single PR and run `python ws_test.py` against a live instance first.
# R11/DEP: upgrades from v1.0.7.5 pins to close:
#   - starlette CVE-2025-62727 (Range header O(n²) DoS — HIGH)
#     → fastapi 0.115.12 pulls starlette ≥0.49.1
#   - starlette CVE-2025-54121 (multipart event-loop block — MED)
#     → fastapi 0.115.12 pulls starlette ≥0.47.2
#   - requests CVE-2024-47081 (.netrc leak on crafted URLs — LOW)
#     → requests 2.33.1
#   - requests CVE-2026-25645 (predictable temp file — LOW, not hit in normal use)
#     → requests 2.33.1
requests==2.33.1
fastapi==0.115.12
uvicorn==0.34.2
websockets==15.0.1
# Optional extras — uncomment or install via `pip install -r requirements-extras.txt`
# anthropic==0.40.0    # Anthropic Claude API fallback (set ANTHROPIC_API_KEY)
