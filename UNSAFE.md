# Unsafe invariant exceptions

Auto-generated by `tests/test_invariants.py::test_allow_comments_have_lifecycle`.
Do not edit manually. Regenerate by running the test suite.

Generated: 2026-04-14  (grace ends 2026-06-01)

## Active allow-comments with lifecycle (0)

(none)

## Grandfathered legacy comments (6)

- agent.py:1014 rule=raw-message (no until=) because=tool_calls is a non-content field the model emits
- compactor.py:155 rule=raw-message (no until=) because=separate summarizer API call, not main agent history
- compactor.py:166 rule=raw-message (no until=) because=separate summarizer API call
- web.py:2091 rule=raw-message (no until=) because=tool_calls is a non-content field the model emits
- worker.py:83 rule=raw-message (no until=) because=CS agent registration payload, not LLM conversation role
- worker.py:251 rule=raw-message (no until=) because=tool_calls is a non-content field the model emits
These predate the governance rule. After the grace date they must gain an `until=YYYY-MM-DD` clause or be removed.

## Expired (test fails) (0)

(none)

